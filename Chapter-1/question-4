fn main() {
   let x=5;
   println!("value of x is:{} ",x);
   {
        //inner scope
       let x=x*2;
       println!("inner scope:{}", x);
   }
   //outer scope
   println!("outer scope: {}",x);
   //shadowing
   let x=x-1;
   println!("value after shadowing:{}",x);
}
